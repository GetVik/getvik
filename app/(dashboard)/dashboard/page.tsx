"use client";

import { MainContent } from "@/components/dashboard/MainContent";

export default function DashboardPage() {
  // Layout handles authentication and role checks
  return <MainContent />;
}